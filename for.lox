print "start test";
// if no initializer, declaring variable outside of for loop runs correctly
// if both declarations exist, enter infinite loop
// if not declared outside loop, fails on resolution of variable x
// var x = 0;
// for (; x < 10; x = x + 1) {

for (var x = 0; x < 10; x = x + 1) {
  print "test in loop";
  print x;
  }
